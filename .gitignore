# Audit output — generated per run, not committed
audit/

# Your filled-in config (keep team/repo details out of git)
audit-config.yml

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Node (Playwright is auto-installed at runtime for PDF rendering / Pass E)
node_modules/
package-lock.json
package.json

# OS / editors
.DS_Store
.vscode/
.idea/
